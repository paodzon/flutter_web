import 'package:flutter/material.dart';

class WorkflowsPage extends StatelessWidget {
  const WorkflowsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Workflows Page'),
    );
  }
}
